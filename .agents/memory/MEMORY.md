- [Foundation freeze](foundation-freeze.md) — 5-layer TRUTH→EVENTS→SIGNALS→MEMORY→STORY + Adjacency Law frozen in docs/canon/05; Signal=Type×Tier; size never raises tier.
- [Tailwind v4 arbitrary colors](tailwind-v4-arbitrary-colors.md) — color-mix arbitrary values compile; `/opacity` on arbitrary var() colors is unreliable — use color-mix or inline style.
- [Mockup preview false-blank](mockup-preview-screenshot-blank.md) — pure-white external_url screenshot of a /__mockup/ preview is a capture timing/cache race, not a bug; bust with `?cb=`, trust the live canvas iframe.
- [Obsidian Cockpit theme doctrine](obsidian-cockpit-doctrine.md) — accent is cyan in DARK (default) only; light theme intentionally stays gold/ivory. Serif (Fraunces) only inside `.editorial-serif`.
- [Syndicate copy canon](syndicate-copy-canon.md) — canonical CTA/status/identity vocabulary; "Join The Syndicate"/"Buy More SYN", LIVE/PARTIAL/PENDING, Member≠Holder≠Collector, Seat≠Seat Record.
- [Archive artifact truth gating](archive-artifact-truth-gating.md) — only assert "mint open" when active===true AND paused===false; pause-unknown must degrade, never imply open; uncapped never gets a supply bar.
- [Self-service ↔ inventory truth](self-service-inventory-truth.md) — "every tier available online" is a truth claim bounded by live availableSyn; Cornerstone (1M SYN) outruns funded inventory → buy UI blocks exceedsInventory + copy says "up to live inventory"; never per-tier hardcode (that's the manual-onboarding framing).
- [my-syndicate doctrine test](my-syndicate-doctrine-test.md) — now pins the NARRATIVE ARC order across route+MemberCockpit; scans COMMENT-STRIPPED source (a tag named in a comment inflates the once-count). Moving a surface between the files breaks the scan even when the page is right — fix the token, not the move.
- [my-syndicate narrative arc](cockpit-narrative-arc.md) — page = ONE arc: cockpit owns Identity→Place→Ownership→Momentum→Action; route owns Memory→Proof→Building. SealingNext moved INTO cockpit; CockpitMemory moved INTO route Memory zone; embed-aware kids need CockpitEmbedProvider.
- [Chapter boundary semantics](chapter-boundary-semantics.md) — a chapter SEALS at endN; the NEXT chapter OPENS at next.startN (=endN+1). "opens at" copy must use startN, never endN.
- [Replit Node deploy](replit-node-deploy.md) — @lovable.dev preset defaults to Cloudflare; pass `nitro:{preset:"node-server"}` for a Node server on Replit (Lovable still forces Cloudflare). Autoscale needs ONE external port in .replit.
- [Cockpit member/visitor gating](cockpit-member-visitor-gating.md) — during idx.isLoading a real member's record is briefly undefined; gate any visitor/"Join now" branch on isLoading or you flash a join CTA at members.
- [Doctrine guard ↔ Authority Map decoupling](doctrine-guard-decoupling.md) — guard uses hardcoded CANONICAL_DOCS/DOC_BANNED, not the map; reclassifying docs in the map has no auto-enforcement until the test arrays are edited (a code pass).
- [Purchase-events canonical scan](purchase-events-canonical.md) — ONE shared getLogs scan feeds ~31 holder-index consumers; keep `limit` OUT of the queryKey + memoize select (useCallback[limit]) or you refork scans / re-derive every render.
- [Cockpit DEV preview fixtures](cockpit-dev-fixtures.md) — DEV-gated wrapper hooks + `?cockpit=` flag preview connected /my-syndicate states; in `src/lib/dev/` (NOT src/lib/preview) to dodge the LIVE-pill guard; never fixture protocol truth. DEV=false in ALL builds → fixtures can't repro prod-only bugs.
- [Wallet-gated SSR hydration gating](cockpit-ssr-hydration-gating.md) — connected-only PROD-only crash ("This page didn't load"); inline `{isConnected && <el>}`/`{record && <el>}` + structural early-returns. NOTE: #418 hydration is RECOVERABLE — the actual fatal was the BigInt throw below, not hydration. Cure: mounted gate centralized in useCockpitAccount; don't remove.
- [BigInt in wagmi query keys](bigint-query-key-hash.md) — direct queryClient.getQueryState/setQueryData on a wagmi key with bigint args throws "Do not know how to serialize a BigInt" (bypasses wagmi's per-query hashFn). Cure: global queryKeyHashFn = wagmi `hashFn` (drop-in). THIS was the real /my-syndicate connected crash.
- [Rank doctrine + enforcement](rank-constitutional-ruling.md) — rank = mutable structural recognition, confers nothing, subordinate to seat+cohort; NOT wealth/reward/leaderboard. ENFORCED in code: scoreMultiplier removed; renames Founder→Vanguard, Genesis Circle→Cornerstone, Patron→Steward, Council Candidate→Custodian, Council→Keystone (Patron=support/Council=governance, reserved); no spend-ladder copy; labs quarantined.
- [Money / Signal guardrail](signal-money-guardrail.md) — money may feed rank/Financial-Trace but NEVER alone raise a Signal tier, make a Chronicle candidate, confer governance, or imply ROI; money-weighted scores quarantined (A/B/C/D). Signal Engine now IN PRESTIGE_MODULES; Rule-E magnitude-token check covers signal-registry.ts only (deriver may read amountUsd).
- [Signals Engine](signals-engine.md) — minimal deterministic Event→Signal layer (Type×Tier×Subject); reads EVENTS only; person subjects ≤S2 (money detects pre-declared crossings only, never sets a person tier); milestones/firsts coverage-gated; windowCoversDeployment is caller-asserted (Sprint 3 must derive it); §3.2 facets ≠ SignalTypes.
- [my-syndicate moved-surface test breakage](my-syndicate-stale-route-tests.md) — route-file guard tests break when a surface moves INTO the cockpit (not deleted); retarget at new home (WhatChangedForYou→CockpitMemory, /nft→NftPage), fact id is "usdc-paid" not "contribution"; arc order pinned by my-syndicate-doctrine. 6 failures RESOLVED.
- [Cockpit progression single surface](cockpit-progression-single-surface.md) — chapter/progress renders once; CockpitProgression is the test-pinned canonical surface — de-dup by removing competing strips, never by slimming it. idx.ordered exists for neighbours.
- [Cockpit co-witness band](cockpit-co-witness-band.md) — Seats Around You uses idx.ordered (neighbours = N±1); neighbour wallets OK (matches /members), never add amounts/rank/standing; Wake+Seats gate member→loading→isError(PARTIAL)→visitor in lockstep — never reorder, change both.
- [Cockpit protocol heartbeat](cockpit-protocol-heartbeat.md) — narrative complement to LivePulseStrip (keep both); event = idx.latest[0]; LIVE pill = freshness, but verb/pulse ("just purchased") gate on actual age (<1h) or you fabricate recency.
- [Shared chain-tip + immutable LP order](global-read-tuning.md) — ONE ["chain-tip"] query (CHAIN_TIP_QUERY_KEY/useChainTip) feeds chain-time+pulse; reuse it, never add a 2nd head fetch; LP token order cached staleTime:Infinity; shared queryFn THROWS not returns undefined (v5).
- [Memory vs activity vs chronicle roles](cockpit-memory-activity-roles.md) — spine (CockpitMemory) / pointer (ActivityStrip) / canon (Chronicle) doctrine unchanged, but ALL THREE now live in the route Memory zone (#memory); roles are the rule, location moved.
- [Dev Invalid hook call on wallet resync](dev-invalid-hook-call.md) — DEV-ONLY Vite dual-dispatcher noise on wallet_account_resync; app code clean, React deduped, zero prod impact — don't re-investigate or re-add vite dedupe.
- [Cockpit embed layout doctrine](cockpit-embed-layout.md) — ONE global MobileJoinBar (never add a 2nd fixed bottom bar); cockpit children use CockpitSection+useCockpitEmbed; ?cockpit fixtures can't see real-wagmi-only surfaces (WhatChangedForYou) — audit those by hand.
- [Purchase-events persistence](purchase-events-persistence.md) — canonical scan cached to localStorage; seed STALE so LIVE (event-block-age) stays truthful; INCREMENTAL cursor (lastScannedBlock + REORG_OVERLAP, merge/dedupe txHash:logIndex, persist complete-scan-only) — template for future scanner blocks; LivePulseStrip LIVE pill hardcoded.
- [Wallet-reads persistence](wallet-reads-persistence.md) — seed wagmi's own cache via q.queryKey+setQueryData(updatedAt=dataUpdatedAt) to keep return shape; NEVER persist allowance (write-gate); archive persist complete-only; buyer totals skipped (holder-index covers).
- [TanStack flat-route layout needs Outlet](tanstack-flat-route-layout-outlet.md) — a parent route file with dotted children is a LAYOUT; no `<Outlet/>` → children silently render the parent. Move index content to `X.index.tsx`; failure is visual, not a type error.
- [One canonical metric registry](dual-truth-registries.md) — protocol-metrics-registry.ts is THE source; truth layer (bind) + verify drawer (fromMetric) + surfaces (metricLabel) all PROJECT from it via aliases. Add/label metrics there only; bind() never silently upgrades status; metricLabel can be undefined → `?? fallback`.
- [Revenue routing by stream](revenue-routing-by-stream.md) — 70/20/10 split is Membership-Sale-only; NFT mint proceeds accrue to Archive1155 owner, LP fees to the Liquidity LP position. VAULT_ASSETS/VAULT_INFLOWS mock but quarantined (gated/labeled /vault preview); fake GENESIS_NFT_PRICE const removed.
- [Canon terminology collisions](terminology-collisions.md) — same word, two concepts: "Patron" (rank $500 vs Patron Seal artifact), "Council" (rank vs governance), "Vault" (Wallet/Reserve/brand/Ledger), "Genesis" (chapter/artifact), "Chronicle" (system/ID9, but BANNED as public vocab → public = "Activity").
- [Authority-map architecture](authority-map-architecture.md) — canon already exists but fragmented: DOCUMENTATION_AUTHORITY_MAP (classes+precedence), UNIFIED_PROTOCOL_DOCTRINE_MAP (4-verb Money/Time/History/Constitution), TERMINOLOGY_GLOSSARY, CANONICAL_REGISTRY; code registries OUTRANK docs in precedence. Consolidate/index, don't rewrite.
- [Canon foundation](canon-foundation.md) — docs/canon/ is the consolidated doctrine front door; authoritative-by-convention but NOT guard-enforced yet (registering it needs banned terms on `**Wrong:**` lines); binding collision rulings incl. Member=sale buyer.
- [as-const config + ternary → never](as-const-config-ternary-never.md) — deriving UI from an `as const` registry can narrow the mapped element to `never` in a provably-dead branch; extract id/active to plain locals before branching.
- [Supply & burn visibility](supply-burn-visibility.md) — burned = balanceOf(0x…dEaD) NOT 1B−totalSupply (transfer-burns keep totalSupply fixed); circulating −= burned; Proof of Burn #001 = pinned card, no new event-kind.
- [Protocol Intelligence Bar](protocol-intelligence-bar.md) — global ticker; Ref Market Cap/FDV use FIXED access rate ($0.01), NOT LP spot (legal framing); status pill derived from fact presence, never hardcoded LIVE.
- [Metric display forms + workbench](metric-display-forms.md) — reuse registry-driven blocks in src/components/metrics/MetricDisplays.tsx; catalog at /labs/protocol-intelligence; LIVE pill can sit next to "—" (= documented ceiling, not value).
- [Protocol event pipeline](protocol-event-pipeline.md) — one path raw→enrichEvent→CanonicalProtocolEvent→effects→Activity→Chronicle candidate→surfaces; adding a kind needs lockstep edits across many maps + tests.
- [check-ownership-wording banlists](check-ownership-wording-banlists.md) — guard NOT in check-release (run it explicitly; tail hides findings); banlist-DEFINING files (FORBIDDEN_LANGUAGE / *_FORBIDDEN forbiddenVocab) false-positive → EXCLUDE_FILES, then restore coverage with a findForbiddenLanguage test over their label/description strings.
- [Economic truth](economic-truth.md) — rank = cumulative USDC (monotonic, recognition only; labs use rankForSyn=balance); Vault Wallet(70% USDC)==Vault Reserve(25% SYN) SAME address 0x205DdC…; no AUTOMATED burn but a real transfer-burn exists (Proof of Burn #001=1,000 SYN→0x…dEaD, burned=balanceOf dead); LP "Burn"=liquidity removal; fake GENESIS_NFT_PRICE/VAULT_STARTING/NEXT_MILESTONE consts REMOVED; VAULT_ASSETS/INFLOWS/HISTORY/useLiveData mock but quarantined (gated /vault preview).
- [Windowed first/earliest claims](windowed-first-claims.md) — never assert protocol-wide "first ever" from a truncated newest-first useProtocolEvents window; default to window-relative wording, gate true firsts behind a completeness flag.
- [Route codegen + vitest OOM](tanstack-route-codegen-and-vitest-oom.md) — new /routes file fails tsc until routeTree.gen.ts regenerates (restart dev workflow); full vitest OOMs → run per-batch in separate vitest processes.
- [Economic significance ruling](economic-significance-ruling.md) — subject decides standing: protocol milestones MAY be S3+; person-money ≤S2, never auto-Historic; rule is person-scoped (05 §4.5).
- [Signal↔Memory adjacency ruling](signal-source-taxonomy.md) — founder ruling: SIGNALS read EVENTS only, never MEMORY (no carve-out); re-express MEMORY facts as EVENTS, never MEMORY→SIGNAL.
- [Institutional Register doctrine](institutional-register-doctrine.md) — PROMOTION→REGISTER durable store (≠published); reads promotion DECISIONS only; member-living excluded; approved→draft, active only if human-finalised+coverageOk. Gotcha: doctrine copy tests match banned terms by SUBSTRING ("roi" in "heroised") — fix the copy, not the test.
- [Chronicle Promotion layer](chronicle-promotion-doctrine.md) — 5th adjacency layer (Chronicle Review Candidate→Promotion Decision); baseline=recommendation (reviewer/timestamp null), precedence copy>coverage>memory rules, member-living ALWAYS hold, identity+amount-blind, member-memory path reserved for DAO ratification; guard-wiring lockstep.
- [Chronicle Review Candidate layer](chronicle-review-candidate-layer.md) — Memory→Chronicle review-ready ONLY (no publish/Story/Recognition); reads MEMORY only; eligibility tier-driven + identity-neutral (S0 never/S1 safe-set/S2+ always); derivers emit only needs-review/hold-*; copy regenerated person-free ("returns" banned, "return" safe).
- [Signal→Memory Candidate layer](memory-candidate-layer.md) — register from SUBJECT (not tier); purchase→membership so gate S1 membership on new-member; founderAction copied onto every signal → branch burn vs funding copy & exclude allocation-movement explicitly; "raised"/"yield" banned.
- [Engine completeness vs pipeline](engine-completion-vs-pipeline.md) — canon 5-layer model is a refinement pipeline w/ no feedback edge; LIVE truth/events/identity/economics but SIGNALS=FUTURE, DIFFUSION loop unassembled, recognition/chronicle candidates PENDING in hidden labs; audit the LOOP not the page list.
- [Canonical layer architecture](canonical-layer-architecture.md) — anti-mixing layer taxonomy: Treasury⊂Economic (70/20/10 = Vault/Liquidity/Operations), Rank=Identity-attr vs Recognition=Memory-output, Artifact≠Seat (until SeatRecord721), Chronicle⊂Memory, founder-action default=Activity not Story.
- [Production build OOM ceiling](build-oom-environment.md) — `vite build` SIGKILLs in transform (cgroup OOM, no error); bigger heap = killed sooner; bg builds need setsid+marker; validate via tsc+vitest+dev-server, not the build.
- [Chronicle two-register classification](chronicle-two-register.md) — register is DERIVED from category via CATEGORY_REGISTER (never set by hand); founder/system subjects → protocol-institutional only + institutional-significance gate; member-living stays organic.
- [Institutional Register public view](institutional-register-public-view.md) — /institutional-register = standalone additive route (nesting needs Outlet/is doctrinally wrong); shows active+draft only; selection leaf is OUTSIDE the adjacency guard so it may import the deriver; SSR renders the LOADING branch (empty/entries are client-only); new banlist file needs EXCLUDE_FILES + a copy test.
- [Protocol Lineage projection](protocol-lineage-projection.md) — lineage is a PURE 1:1 re-expression of a ChronologyEntry's carried trail (type-only chronology-registry), NOT a new layer; genesis/unknown→PARTIAL; date only on verified chronology node; not a knowledge-map node (add internalSurfaces instead).
- [Chronology layer doctrine](chronology-layer-doctrine.md) — Entry→Timeline overlay orders by verified block ONLY; tx proves existence not order (held); sequenceNumber positional (chronologyId durable); blockTimestamp now THREADED as verified metadata via pure overlay (Sprint 15) but NEVER an ordering input, only verified path carries a date; chapter/milestone null; I/O hook (chronology-timestamps.ts) is the one wagmi-allowed module (own adjacency describe); chain-time is current-tip only, never reuse for historical blocks.
- [Chronicle Entry layer](chronicle-entry-layer.md) — terminal admission→entry edge: draft-only, resolvePublication NEVER emits published, copy>rules re-validated from source, register=proposedChronicle*, sourceBlock omitted (honest); maintainer-note arrays are length-pinned by their guard test; docs/ exempt from wording guard.
- [Chronicle Admission layer](chronicle-admission-layer.md) — last edge (Institutional Register Entry→Chronicle Admission Candidate); inspection-only eligibility, never publishes; precedence COPY>COVERAGE>RULES, member-living excluded P0, keys on rule BUCKET not amount; deriver must NOT import institutional-register deriver/genesis (merged at route); knowledge-map adds surface to EXISTING nodes (new node id forces a canon-doc edit).
- [Protocol Knowledge Map](protocol-knowledge-map.md) — code registry protocol-knowledge-map.ts is THE source (zero-import leaf); doc 09, internal /labs/knowledge-map AND public /knowledge-map all project from it. Public map = DERIVED 3 buckets (live-projection/durable-overlay/reserved), publicSurfaces only (never internalSurfaces), structure not live values; recognition=partial→live bucket. Protocol Knowledge=live projection vs Register Memory=durable assertion+anchor; promote-or-seed-else-held. New public route needs a sitemap entry.
- [Institutional Register genesis seed](institutional-register-genesis-seed.md) — register has a 2nd lawful source (genesis seed leaf) for pre-scanner facts; CONFIG→SEED edge, merge dedups by tx (seed wins); genesis word allowed only verified/locked (findHistoricClaims is verification-aware); held-not-invented ordinals.
- [check-live-content prod gate](check-live-content-prod-gate.md) — a FAMILY (check-live-content.mjs gated; check-homepage-content.mjs ungated) FETCHES the DEPLOYED site, not local; a lone red there = republish needed, not a local regression. Trust local tsc + vitest batches + visitor-vocabulary + ownership-wording.
- [Public Chronicle integration](public-chronicle-integration.md) — locked /chronicle gains "Institutional record" backing from ACTIVE genesis-seed entries (NOT drafts — would invert publication seam); curated overlap map, active-only gate, never re-list non-overlapping facts (no 2nd feed); keep chronicle-doctrine.test.ts separate+untouched.
- [Homepage gate pins](homepage-gate-pins.md) — index.tsx must keep <ProtocolStorySoFar>+<StoryTimeline> (gated vitest); Flywheel emits required live phrases; check-attention-hierarchy/check-loop-ownership are stale & ungated decoys; cut HomeArchiveTeaser → restore an /archive link.
- [Hero throne/ring layout](hero-radial-throne-layout.md) — radial-engine composition+perf doctrine: seat-text ABOVE ring, throne integrated w/ glow, 6 nodes on symmetric outer ring; abspos node w/ only `left` wraps into a sliver → `whitespace-nowrap`; reduced-motion gated in 3 places (dots/AnimatedNumber/SVG animateTransform); hero bitmaps→WebP+preload+img width/height; bg cervin.webp both themes; connected-header compact `…XXXX` below 2xl fits @1280.
- [Hero last-activity age estimate](hero-last-activity-age-estimate.md) — hero "Nd ago" = (head−lastPurchaseBlock)×flat 2s, NOT real block.timestamp; explorer shows truth → ~2× drift; not stale-cache/wrong-tx/formatter.
- [Hero right-panel division](hero-right-panel-division.md) — radial diagram OWNS money stats (Next seat/USDC routed/Vault/Liquidity/Operations); Protocol Overview panel = identity/token/supply only (don't re-add them). HeroActivityRail status is source-aware PARTIAL (stricter than feed's hardcoded LIVE — keep it).
- [Proof of Burn rename](proof-of-burn-rename.md) — founder override of canon: burn term "Proof of Fire"→"Proof of Burn". RULE: any RENDERED token (labels, display ids/slugs/kind codes, metric ids) = "burn"; only never-rendered code identifiers may stay "fire". Flame reverted amber→bonfire.
- [Eras + packages distribution layer](eras-packages-distribution-layer.md) — infinite-game journey is additive FE/config/docs only; eras (distribution) = 2nd coord system over untouched chapters (story); only Genesis LIVE, II–IX PROPOSED; packages project 1:1 from RANKS_V2; cockpit CTA never flashes Join at a loading member.
- [Model 2 identity continuity](model2-identity-continuity.md) — V2-sells-Genesis-#3–333 preserves identity/chapters/milestones with NO further contract change; 2 wiring gates (V2 emits `Purchased` not `TokensPurchased`; V1-proof write path) + don't edit pure #333 boundaries.
- [genesisOffset 333 floor (Model 2)](genesis-offset-333-floor.md) — LOWER floor REMOVED; only `>=FINAL_SEAT` reverts. 333 = chapter/Era-I→II boundary only, NOT a deploy gate. Sub-333 deploys OK: V2 continues Genesis, Era II opens at #334 via range (constructor forces eraSynCap[1]=max; eraCaps[0] IGNORED). Stale "208,125 Genesis cap" figure superseded.
- [Sale V2 addendum doctrine](sale-v2-addendum.md) — DRAFT-only (doc §L+§R embed .sol byte-identical); per-era SYN sold-cap over ONE global balance is the only non-bypassable anti-Sybil throttle → seat no longer fixes price (eras.ts = positional preview); recovery timelocked; no oracle; "First Million" = honest target not guarantee. Param sim: ship Model B (~248M SYN/71%); single global per-address cap is provably wrong → NOW uint256[9] per-era in draft; reserve = configurable RESERVE_THROUGH_SEAT, Σ seats×own-era min-rate (130.9M full / 3.9M default #10k II–IV), NOT current-era blanket. REFERRAL now = external CommissionRouter V1 (sale routes the 10% Ops slice; tiered 30–80% OF that slice, not flat 5%; fallback never bricks buy).
- [Share-card PNG export pattern](share-card-export-pattern.md) — capture target (nodeRef) must be the position:relative, opaque, fixed-dimension, inline-styled card; only the OFF-SCREEN wrapper is position:fixed left:-100000; no external images/web-fonts; can't auto-smoke without a connected member wallet.
- [Dev RPC CORS / cold-scan blank](dev-rpc-cors-cold-scan.md) — dev preview shows PENDING/— (no on-chain data) bc primary api.avax.network = 429 + NO CORS header, and a cold full holder-index scan rate-limits the ankr fallback; prod ok (seed+persisted incremental cache + ankr CORS:*). NOT a code bug — don't "fix" wagmi.
- [RPC env-override (replace, not additive)](rpc-env-override.md) — VITE_AVALANCHE_RPC_PRIMARY/_FALLBACK override the public defaults in syndicate-config (exports unchanged → wagmi/chain-registry/health/og-data untouched); override REPLACES, never additive; don't add api.avax.network as tertiary without scope approval; VITE_=public.
- [Whitepaper extraction map](whitepaper-extraction-map.md) — docs/whitepaper/WHITEPAPER_EXTRACTION_MAP.md = source-of-truth index, NOT canon (code wins); Architect $250-below-Steward $500 order is DELIBERATELY deferred — a rename is governance, not a copy tweak.
- [Sale V2 deploy doctrine](sale-v2-deploy-doctrine.md) — founder ruling: owner = founder/admin EOA (NOT multisig; Ledger/multisig=future hardening, not a blocker); NO Fuji/testnet → local/forked-mainnet rehearsal then mainnet-direct tiny-funding-first; deploy-blocker list intact; .sol "multisig" comments left as advisory.
- [Member vs wallet route roles](member-vs-wallet-route-roles.md) — /member/N = human surface (pure useHolderIndex projection, no connected-wallet reads → SSR-safe); /wallet/$address = verification surface; share cards prefer /member/N URL; both dynamic → never in sitemap.
- [Identity hierarchy freeze](identity-hierarchy-freeze.md) — RATIFIED master identity = Holder Index (derived from TokensPurchased first-seen across ALL sale contracts); raw memberNumberOf is never identity. Only events+minted NFTs are permanent; SeatRecord721 MUST mint from index (proof/trusted), NEVER raw sale numbering = the one irreversible gate. M2 = one-seat enforced operationally (buy-UI v1Proof) not on-chain. Verdict: SAFE TO FREEZE.
- [contracts/ Foundry harness](contracts-foundry-harness.md) — isolated Foundry project for production-candidate Sale V2 + Router V1; env path/.bashrc-denied quirks, via_ir REQUIRED (buy() stack), Merkle leaf == StandardMerkleTree["address"], hardening scope + deferred M1/M2/timelock.
- [V2 Purchased⋈Routed fail-closed join](v2-purchased-routed-join.md) — missing money-split log = incomplete data, NOT a real zero; drop+joinIncomplete+gate persistence, never `?? 0`. Template for any multi-log join. Dormant-V2 short-circuit + source-scan tests are companion gotchas.
- [Fork rehearsal env](fork-rehearsal-env.md) — Sale V2 fork rehearsal: archive RPC + `--evm-version cancun` (CLI not foundry.toml) + one-shot anvil orchestration (never `pkill -f anvil`=self-kill 143); Deploy.s.sol fails closed on provisional snapshot.
- [LivePurchase two-step UX](livepurchase-two-step-ux.md) — approve & buy() are SEPARATE signatures (explicit stepper + recovery banner); tri-state allowance (undefined=loading) makes needsApprove false → gate on walletReady; recovery banner must be member-aware.
