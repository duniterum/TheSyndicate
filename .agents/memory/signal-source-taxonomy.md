---
name: Future Signal Engine source taxonomy
description: The unresolved SIGNALS↔MEMORY adjacency tension exposed by the founder's approved FUTURE signal sources.
---

The founder approved (FUTURE direction; recorded in canon `07_FOUNDER_PRINCIPLE.md` FS-1) that the future
Signal Engine will read signals grouped as Financial / Protocol / Memory Trace — *source groupings* that
fold into the frozen seven Signal Types (canon 05 §3.2), NOT a new architecture. Signals = interpretation
("why does this matter?"); the EVENT layer stays the source of truth. (Full taxonomy lives in 07 FS-1 —
don't duplicate it here. Note: FS-1's "Financial Trace" = a *signal-source grouping*; the Financial Trace
*data record* of canon 06/08 is a different object — referral is a Protocol-Trace signal here but a
Financial-Trace record field there.)

**The durable trap.** Two of those sources — **Chronicle appearance** and **recognition milestone** — are
MEMORY-layer outputs, and MEMORY is *downstream* of SIGNALS in 05's Adjacency Law
(TRUTH→EVENTS→SIGNALS→MEMORY→STORY). Reading them as signal inputs means SIGNALS reading MEMORY, which is
NOT sanctioned (the only carve-out is MEMORY ingesting EVENTS). 05 §3.2's "Historic" type (sourced from
Chronicle) has the identical tension.

**Why it matters / how to apply.** This is an unresolved founder decision, not an oversight to "fix." Do NOT
quietly let SIGNALS read MEMORY. Resolve by one of: (a) compute those at the MEMORY layer as Recognition;
(b) re-express the underlying fact as an EVENT the Signal layer can read; or (c) ratify a new explicit
Adjacency carve-out in 05. Resolve the "Historic" type together with these.
