---
name: Canonical protocol layer architecture
description: Definitive layer taxonomy + the classification rulings that stop layer-mixing (Treasury⊂Economic, Rank vs Recognition, Artifact≠Seat, Chronicle⊂Memory, founder-action default).
---

# Canonical layer model (the anti-mixing map)

**Top-level layers:** TRUTH (substrate) · IDENTITY · ECONOMIC (Treasury sublayer) · ARTIFACT · EVENTS/ACTIVITY · SIGNALS · MEMORY (Chronicle + Recognition sublayers) · STORY · ANTICIPATION · DIFFUSION (Growth/Referral sublayer) · INTELLIGENCE (cross-cutting, read-only derived) · FUTURE (Governance/Marketplace/Ecosystem/SeatRecord721).

## Binding classification rulings (keep consistent)
- **Treasury is a SUBLAYER of Economic, never its own top layer.** Split enforced on-chain in the Membership Sale contract: **Vault = 70%, Liquidity = 20%, Operations = 10%** of every USDC purchase; the **Membership Distribution Wallet** holds the SYN inventory delivered to buyers. Protocol value-holding wallets = vault/liquidity/operations/distribution (`known-addresses` `isProtocolWallet`).
- **Two distinct "recognition" concepts — never merge:** (1) **RANK = an IDENTITY attribute** (cumulative USDC, recognition-only); (2) the **RECOGNITION SYSTEM** (early/continuity/witness) = a **MEMORY OUTPUT** sublayer. Money may raise Rank-recognition but confers nothing economic back.
- **ARTIFACT is its own layer, distinct from Identity and Memory.** Seat = Identity (derived, abstract, always exists, NOT an NFT). Archive1155 / First Signal / Patron Seal = held Artifacts (optional, not required for membership). SeatRecord721 (PENDING, address null) is the FUTURE point where Identity becomes an ownable Artifact — until it ships, **NFT ≠ Seat**.
- **Chronicle is a CURATED SUBSET of Memory, not its own layer.** Spine: Activity records everything → Signals detect what matters → Memory preserves → Chronicle = the meaningful canon subset → Story explains. One-way; Chronicle never invents. "Chronicle" is BANNED public vocab → public = "Activity"/"Story".
- **Founder/system wallet action default = ACTIVITY only, NOT Story.** Promotion to Signal/Chronicle/Story needs a Signal rule over event properties — not the mere fact the founder acted. Rare historic exceptions: token/contract deployment, initial liquidity, founder burn (Proof of Fire). Routine vault/operations/LP movements stay Activity-only. (`founder-actions.ts` categories: founder-burn / funded-operations / funded-vault / funded-liquidity / allocation-movement; classified ONLY when sender = founder wallet; ops/liquidity inflows reserved, not scanned yet.)
- **Anticipation is a SIBLING of Story (forward), fed by Truth + locked config, NOT Signals** (see engine-completion-vs-pipeline rulings).
- **Wallet ≠ one concept.** Roles: identity (user wallet) · payment-source (user wallet, transiently) · custody/treasury (vault/liquidity/operations/distribution) · founder-actor (founder wallet, public/vested) · artifact-holder (any wallet holding Archive1155). Same address can hold multiple roles, but a user wallet is NEVER treasury.

## Touch boundaries (which layer you are editing)
- **Share cards / OG** → DIFFUSION only; they CONSUME Identity/Economic/Truth read-only — don't modify the source layers.
- **Vault** → ECONOMIC › Treasury (`treasury-hooks`, contract-registry wallet entries, `syndicate-config` addresses); don't touch Activity/Story.
- **MemberCard** → Identity public surface (its data) + Diffusion (its share/export).
- **Chronicle** → MEMORY › Chronicle (`chronicle-entries`/`chronicle-candidates`) + Story surfaces; must derive from Activity/Signal — never hardcode new "facts."
- **NFTs** → ARTIFACT (`archive-*` modules, `archive-id-registry`, MythologyWall, NftPage) + Truth registries; don't touch Seat/Identity (NFT ≠ Seat).

**Why:** the recurring failure mode is concept-mixing (money↔identity, NFT↔seat, activity↔chronicle, founder-action↔story). This map is the precedence guard so future work edits ONE layer and consumes the others read-only.

## Final architecture challenge rulings (pre-freeze)
- **Artifact stays an INDEPENDENT Tier-2 (Truth-rooted) layer — NOT a child of Memory→Story.** A LIVE layer (Archive1155 mintable now) cannot be a child of FUTURE layers (Signals/Memory); mint/own/transfer/verify are STATE verbs, not "preserve significance"; artifacts are transferable, memory is not; Patron Seal carries no memory. `Memory→Story→Artifact` is a **provenance edge (issued-in-commemoration-of), NOT containment** — like Diffusion *reads* Identity without being part of it. Two artifact kinds: **native** (Patron Seal / support / collectible — no memory) vs **commemorative** (future chapter/milestone artifacts, SeatRecord721 — *reference* a Memory/Story moment; citation must link to verifiable fact).
- **Chronicle preserves PROTOCOL memory (option B), not member-only.** Unit = the significant *protocol moment*, any actor. Two registers: **Genesis/Protocol** (deployment, first liquidity, first burn, milestones — subject=protocol, may be S3 Historic) + **Member/Living** (the growing bulk). Significance decided by a SIGNAL, never by actor identity; founder entries recognition-only, never glorification. Genesis events are **seeded as verified Truth-backed facts (tx hash)**, not pulled from the windowed live scanner (windowed-first-claims). The seven genesis firsts (token deploy, sale deploy, first liquidity, first member, first burn, first artifact, first milestone) occupy **all four spine layers Activity→Signal→Chronicle→Story**; *first member* also opens the member register, *first milestone* also feeds Anticipation.
- **There IS a hidden top layer — the CONSTITUTION / GOVERNANCE layer (Layer 0) — but it sits on a DIFFERENT AXIS, not "above Truth."** Two orthogonal axes: **epistemic** (what is TRUE → Truth highest; "code outranks docs" is the anti-lie rule for FACTS) vs **normative** (what is permitted / what it means / what may exist → Constitution highest). No paradox — Truth answers "what is the case," Constitution answers "what is allowed/means." The **Information Hierarchy is one ARTICLE of the Constitution** (the clause subordinating docs to chain on facts); a constitution mandating its own falsifiability-by-chain is self-consistent (it binds itself). NOT "merely documents": it lives partly in prose (canon/replit.md) and partly **compiled into enforcement** (doctrine-guard tests, registry `address:null`/status vocab, money guardrail) — enforcement is PARTIAL (canon authoritative-by-convention, not fully guarded). Render as an enclosing frame around all 12 layers: "governs meaning & permissions; subordinate to Truth on facts."
- **Why (these three):** resolves the recurring "code outranks docs vs doctrine governs everything" confusion — different axes, so both hold; and prevents folding a LIVE state layer (Artifact) under a FUTURE meaning layer.

## Diffusion build-state (Sprint-1 execution lock)
- **Sprint 1 (close the Member→Share→Visitor loop + thin Anticipation) is ASSEMBLY, not new build.** Already fully built & reusable: `ShareActions` (PNG/X/Telegram/copy, no Web Share API by design), `/wallet/$address` (on-chain-verifying public page WITH ShareActions), `/milestone/$id`, both OG routes `/api/public/og/{wallet,milestone}`, `x.tx.$hash` redirect, post-purchase `SuccessReceipt` inside `LivePurchase`. **The only genuinely NEW behavior is attribution-only `?ref` capture** (read param → persist → surface attribution; NO reward, NO contract — `future-referral.ts` already forces rewardStatus PENDING).
- **MemberCard merge data-source TRAP:** canonical `MemberCard` reads `holder-index` (THE Identity source); labs `ShareableCards` reads `sale-hooks` (direct wagmi) = a SECOND identity truth. Any merge/promote MUST converge on holder-index, never sale-hooks, or Identity truth forks. `ShareableCards` + `HomeShareCTA` live in `src/labs/` and are NOT mounted on main routes (promotion candidates).
- **Anticipation is DUPLICATED across surfaces** (chapter progress: HomeNextMilestone vs CockpitProgression; milestone progress: MilestoneApproachingTile vs ActivityMilestones, both via `evaluateMilestones`; next-move: YourNextAction vs CockpitActionRail). Consolidate by REMOVING competing strips, never by slimming the test-pinned `CockpitProgression` (see cockpit-progression-single-surface).

## Object-model freeze — identity triad
- **SEAT is the atom · MEMBER is a binding · WALLET is the key. Never collapse them.** **Seat** = Identity's durable unit (the thing that persists and carries Member#, Chapter, Rank). **Member** = the *relationship* a wallet holds to a seat (wallet↔seat binding; a Member is a sale buyer who crossed the membership threshold) — NOT the wallet and NOT the seat. **Member#** = the seat's permanent ordinal (join order), never re-assigned. **Wallet** = the cryptographic key that PROVES the binding (Identity role); a wallet is not a Member until it holds a seat. **Rank/Chapter = attributes OF the seat** (recognition / era), not of the wallet. **SeatRecord721 (future, PENDING)** would make the seat itself an ownable Artifact = the ONLY bridge Identity↔Artifact. **Why:** the freeze exists so future work never re-defines Member/Seat/Wallet; collapsing them (e.g. "wallet = member", "NFT = seat") is the top drift risk.
